---
- description: "Update 1 feature 1 field in central"
  sequence:
    - type: query
      database: test
      sql:
        UPDATE "test"."pluviometers" SET
        nom = concat(nom, ' by central')
        WHERE id = 0;
    - type: sleep
    - type: synchro
      from: lizsync_clone_a
